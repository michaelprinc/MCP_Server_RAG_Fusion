# Environment template for local development.
# Copy to `.env` and adjust values as needed.

PORT=8080
INDEX_DIR=./data/indexes
MODEL_CONFIG=./configs/model-config.yml
MCP_TOOLS=./configs/mcp-tools.json
ROUTER_MODE=classifier
LOG_LEVEL=INFO
CACHE_SIZE=1000
OCR_LANGUAGES=eng
EMBEDDING_MODEL=BAAI/bge-m3
API_KEY_FILE=
